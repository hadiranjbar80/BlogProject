@model ApplicationUser

@{
    ViewData["Title"] = "";
}
<div>

    <p>
        آیا از حذف @Model.UserName مطمئن هستید ؟
    </p>

    <form asp-action="DeleteUser">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="حذف" class="btn btn-danger" />
    </form>
</div>
